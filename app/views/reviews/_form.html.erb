<%= form_for ([@space, @review]) do |f| %>

	<% if @review.errors.any? %>
	  <div id="error_explanation">
	    <h2><%= pluralize(@review.errors.count, "error") %> prohibited this review from being saved:</h2>

	    <ul>
	    <% @review.errors.full_messages.each do |msg| %>
	      <li><%= msg %></li>
	    <% end %>
	    </ul>
	  </div>
	<% end %>
	
	<p><%= f.label :rating %>
	<%= f.select :rating, (1..5) %>
	</p>

	<p><%= f.label :body %>
	<%= f.text_area :body %></p>
	
	<%= f.submit %>
<% end %>